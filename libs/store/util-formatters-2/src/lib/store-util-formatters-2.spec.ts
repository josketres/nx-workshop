import { storeUtilFormatters2 } from './store-util-formatters-2';

describe('storeUtilFormatters2', () => {
  it('should work', () => {
    expect(storeUtilFormatters2()).toEqual('store-util-formatters-2');
  });
});
